name: Hello World Workflow

on: push

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - name: Print Hello World
      run: echo "Hello, World again!"
    - name: Step1
      run: echo "Hello, World again!"
